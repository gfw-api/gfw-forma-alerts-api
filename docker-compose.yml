prod:
  image: vizzuality/gfw-forma-alerts-api:2.1.0${STAGING}
  ports:
    - "3600"
  env_file:
    - prod.env
  command: start
  dns: ${DNS_SERVER}
  log_driver: "gelf"
  restart: always
  log_driver: "syslog"
  log_opt:
    syslog-address: ${SYSLOG_ADDRESS}
    tag: gfw-forma${STAGING}
